<div class="container form">
  <div class="row">
    <div class="col center-content">
      <EditForm Model="@viewModel" OnValidSubmit="@HandleValidSubmit">
        <div class="mb-5">
          <h3 class="text-center text-white">Forgot Password</h3>
        </div>
        <div class="mb-3">
          <label class="text-white d-block mt-2">Enter your username and email. An admin will contact you shortly.</label>
        </div>
        <div class="mb-5">
          <!-- <label class="form-label text-white">Email Address:</label> -->
          <span class="oi oi-person text-white"></span> <InputText type="username" placeholder="Username" @bind-Value="viewModel.Username" />
          <br />
          <br />
          <span class="oi oi-person text-white"></span> <InputText type="email" placeholder="Email" @bind-Value="viewModel.Email" />
        </div>
        <CustomButton ButtonType="submit" Text="Submit"></CustomButton>
      </EditForm>
    </div>
  </div>
</div>

<style>
  .form {
    display: block;
    text-align: center;
    margin-left: auto;
    margin-right: auto;
    background: linear-gradient( 180deg, rgba(133,47,147) 0%, #673AB7 70%);
    width: fit-content;
    padding: 25px;
    border-radius: 25px;
  }

  .main {
    display: flex;
    flex-direction: column;
  }

  .content {
    flex: 1;
    align-items: center;
    display: flex;
  }

  .page {
    min-height: 100vh;
  }

  .center-content {
    display: flex;
    justify-content: center;
  }

  .form input {
    background: transparent;
    border: none;
    border-bottom: 1px solid #3a2b2b;
    outline: none;
    color: #FFF;
  }

  /* https://stackoverflow.com/questions/44688186/how-can-i-change-the-placeholder-text-color-to-white */
  input::placeholder {
    color: #fff;
  }

  ::-webkit-input-placeholder { /* Chrome/Opera/Safari */
    color: #fff;
  }

  ::-moz-placeholder { /* Firefox 19+ */
    color: #fff;
  }

  :-moz-placeholder { /* Firefox 18- */
    color: #fff;
  }

  :-ms-input-placeholder { /* Edge/IE 10+ */
    color: #fff;
  }
</style>

@code {
  private ForgotPasswordViewModel viewModel = new();

  private void HandleValidSubmit()
  {

  }
}